[project]
name = "gee-for-relative-risk"
version = "0.1.0"
description = "Reproducible RR-GEE, RR-PGEE, OR-GEE, and UK Biobank analyses."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "matplotlib",
    "nibabel",
    "nilearn>=0.14.0",
    "numpy",
    "pandas",
    "pyreadr",
    "rdata",
    "scipy",
    "seaborn",
    "statsmodels",
]

[tool.uv]
package = false
